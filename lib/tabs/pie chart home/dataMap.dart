// ignore_for_file: omit_local_variable_types

import 'package:collection/collection.dart';

import '../../pie/languages/R.dart';
import '../../pie/languages/batch_file.dart';
import '../../pie/languages/c.dart';
import '../../pie/languages/cSharp.dart';
import '../../pie/languages/coffeescript.dart';
import '../../pie/languages/cpp.dart';
import '../../pie/languages/css.dart';
import '../../pie/languages/dart.dart';
import '../../pie/languages/empty.dart';
import '../../pie/languages/erlang.dart';
import '../../pie/languages/go.dart';
import '../../pie/languages/haskell.dart';
import '../../pie/languages/html.dart';
import '../../pie/languages/image.dart';
import '../../pie/languages/java.dart';
import '../../pie/languages/javascript.dart';
import '../../pie/languages/json.dart';
import '../../pie/languages/lua.dart';
import '../../pie/languages/markDown.dart';
import '../../pie/languages/matlab.dart';
import '../../pie/languages/objectiveC.dart';
import '../../pie/languages/perl.dart';
import '../../pie/languages/php.dart';
import '../../pie/languages/powershell.dart';
import '../../pie/languages/python.dart';
import '../../pie/languages/ruby.dart';
import '../../pie/languages/rust.dart';
import '../../pie/languages/scala.dart';
import '../../pie/languages/shell.dart';
import '../../pie/languages/sql.dart';
import '../../pie/languages/swift.dart';
import '../../pie/languages/tex.dart';
import '../../pie/languages/text.dart';
import '../../pie/languages/toml.dart';
import '../../pie/languages/typescript.dart';
import '../../pie/languages/yaml.dart';
import '../pie chart Saved/Saved.dart';
import 'languages_list.dart';

/// ================ batchFile

List<double> batchFile = [];

Future<List<double>> batch_file_() async {
  BatchFileSnips launch = BatchFileSnips(api: api);
  double batch_file = await launch.run();
  double batchCount = batch_file;
  batchFile.add(batchCount);
  return batchFile;
}

/// ================ c

List<double> c = [];

Future<List<double>> c_() async {
  C__Snips launch = C__Snips(api: api);
  double cDpuble = await launch.run();
  double c_Count = cDpuble.toDouble();
  c.add(c_Count);
  return c;
}
// /// ================ c#
//

List<double> cSharp = [];

Future<List<double>> cSharp_() async {
  CSharp__Snips launch = CSharp__Snips(api: api);
  double cPPdub = await launch.run();
  double cPPCount = cPPdub.toDouble();
  cSharp.add(cPPCount);
  return cSharp;
}

/// ================ coffescript

List<double> coffee = [];

Future<List<double>> coffee_() async {
  Coffee__Snips launch = Coffee__Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  coffee.add(batchCount);
  return coffee;
}

//
// /// ================ c++
//
List<double> cPP = [];

Future<List<double>> cPP_() async {
  CPlusPlus__Snips launch = CPlusPlus__Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  cPP.add(batchCount);
  return cPP;
}
//

//
// /// ================ css

List<double> cSS = [];

Future<List<double>> cSS_() async {
  CSS__Snips launch = CSS__Snips(api: api);

  /// batchfile count
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  cSS.add(batchCount);
  return cSS;
}

// /// ================ dart

List<double> dart = [];

Future<List<double>> dartCount() async {
  Dart__Snippets launch = Dart__Snippets(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  dart.add(batchCount);
  return dart;
}

// /// ================ ERLANG

List<double> erlang = [];

Future<List<double>> erlang_() async {
  Erlang__Snips launch = Erlang__Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  erlang.add(batchCount);
  return erlang;
}

/// ================ GO

List<double> go = [];

Future<List<double>> go_() async {
  Go__Snips launch = Go__Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  go.add(batchCount);
  return go;
}

/// ================ HASKELL

List<double> haskell = [];

Future<List<double>> haskell_() async {
  haskell__Snips launch = haskell__Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  haskell.add(batchCount);
  return haskell;
}

/// ================ HTML

List<double> html = [];

Future<List<double>> html_() async {
  html__Snips launch = html__Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  html.add(batchCount);
  return html;
}

/// ================ IMAGE

List<double> image = [];

Future<List<double>> image_() async {
  image__Snips launch = image__Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  image.add(batchCount);
  return image;
}

/// ================ JAVA

List<double> java = [];

Future<List<double>> java_() async {
  java__Snips launch = java__Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  java.add(batchCount);
  return java;
}

/// ================ JSON

List<double> j_son = [];

Future<List<double>> json_() async {
  json__Snips launch = json__Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  j_son.add(batchCount);
  return j_son;
}

/// ================ JAVASCRIPT

List<double> javascript = [];

Future<List<double>> javascript_() async {
  javaScript__Snips launch = javaScript__Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  javascript.add(batchCount);
  return javascript;
}

/// ================ LUA

List<double> lua = [];

Future<List<double>> lua_() async {
  lua_Snips launch = lua_Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  lua.add(batchCount);
  return lua;
}

/// ================ LUA

List<double> markdown = [];

Future<List<double>> markdown_() async {
  markdown_Snips launch = markdown_Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  markdown.add(batchCount);
  return markdown;
}

/// ================ matlab

List<double> matlab = [];

Future<List<double>> matlab_() async {
  matlab_Snips launch = matlab_Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  matlab.add(batchCount);
  return matlab;
}

/// ================ objective_c

List<double> objective_c = [];

Future<List<double>> objective_c_() async {
  objective_c_Snips launch = objective_c_Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  objective_c.add(batchCount);
  return objective_c;
}

/// ================ perl

List<double> perl = [];

Future<List<double>> perl_() async {
  perl_Snips launch = perl_Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  perl.add(batchCount);
  return perl;
}

/// ================ php

List<double> php = [];

Future<List<double>> php_() async {
  php_Snips launch = php_Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  php.add(batchCount);
  return php;
}

/// ================ powerShell

List<double> powerShell = [];

Future<List<double>> powerShell_() async {
  powerShell_Snips launch = powerShell_Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  powerShell.add(batchCount);
  return powerShell;
}

/// ================ python

List<double> python = [];

Future<List<double>> python_() async {
  python_Snips launch = python_Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  python.add(batchCount);
  return python;
}

/// todo ===========
/// ================ r

List<double> r = [];

Future<List<double>> r_() async {
  r_Snips launch = r_Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  r.add(batchCount);
  return r;
}

/// ================ ruby

List<double> ruby = [];

Future<List<double>> ruby_() async {
  ruby_Snips launch = ruby_Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  ruby.add(batchCount);
  return ruby;
}

/// ================ rust

List<double> rust = [];

Future<List<double>> rust_() async {
  rust_Snips launch = rust_Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  rust.add(batchCount);
  return rust;
}

/// ================ scala

List<double> scala = [];

Future<List<double>> scala_() async {
  scala_Snips launch = scala_Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  scala.add(batchCount);
  return scala;
}

/// ================ shell

List<double> shell = [];

Future<List<double>> shell_() async {
  shell_Snips launch = shell_Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  shell.add(batchCount);
  return scala;
}

/// ================ sql

List<double> sql = [];

Future<List<double>> sql_() async {
  sql_Snips launch = sql_Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  sql.add(batchCount);
  return sql;
}

/// ================ swift

List<double> swift = [];

Future<List<double>> swift_() async {
  swift_Snips launch = swift_Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  swift.add(batchCount);
  return swift;
}

/// ================ tex

List<double> tex = [];

Future<List<double>> tex_() async {
  tex_Snips launch = tex_Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  tex.add(batchCount);
  return tex;
}

/// ================ text

List<double> text = [];

Future<List<double>> text_() async {
  text_Snips launch = text_Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  text.add(batchCount);
  return text;
}

/// ================ tex

List<double> toml = [];

Future<List<double>> toml_() async {
  toml_Snips launch = toml_Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  toml.add(batchCount);
  return toml;
}

/// ================ typeScript

List<double> typeScript = [];

Future<List<double>> typeScript_() async {
  typeScript_Snips launch = typeScript_Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  typeScript.add(batchCount);
  return typeScript;
}

/// ================ yaml

List<double> yaml = [];

Future<List<double>> yaml_() async {
  yaml_Snips launch = yaml_Snips(api: api);
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  yaml.add(batchCount);
  return yaml;
}

/// ================ empty

List<double> empty = [];

Future<List<double>> empty_() async {
  empty_State launch = empty_State();
  double bats = await launch.run();
  double batchCount = bats.toDouble();
  empty.add(batchCount);
  return empty;
}

int key = 0;
final dataMap = <String, double>{
  '${Languages.elementAt(0)}: ${batchFile.reversed.first}': batchFile.reversed.first,
  '${Languages.elementAt(1)}: ${c.reversed.first}': c.reversed.first,
  '${Languages.elementAt(2)}: ${cSharp.reversed.first}': cSharp.reversed.first,
  '${Languages.elementAt(3)}: ${coffee.reversed.first}': coffee.reversed.first,
  '${Languages.elementAt(4)}: ${cPP.reversed.first}': cPP.reversed.first,
  '${Languages.elementAt(5)}: ${cSS.reversed.first}': cSS.reversed.first,
  '${Languages.elementAt(6)}: ${dart.reversed.first}': dart.reversed.first,
  '${Languages.elementAt(7)}: ${erlang.reversed.first}': erlang.reversed.first,
  '${Languages.elementAt(8)}: ${go.reversed.first}': go.reversed.first,
  '${Languages.elementAt(9)}: ${haskell.reversed.first}': haskell.reversed.first,
  '${Languages.elementAt(10)}: ${html.reversed.first}': html.reversed.first,
  '${Languages.elementAt(11)}: ${image.reversed.first}': image.reversed.first,
  '${Languages.elementAt(12)}: ${java.reversed.first}': java.reversed.first,
  '${Languages.elementAt(13)}: ${j_son.reversed.first}': j_son.reversed.first,
  '${Languages.elementAt(14)}: ${javascript.reversed.first}': javascript.reversed.first,
  '${Languages.elementAt(15)}: ${lua.reversed.first}': lua.reversed.first,
  '${Languages.elementAt(16)}: ${markdown.reversed.first}': markdown.reversed.first,
  '${Languages.elementAt(17)}: ${matlab.reversed.first}': matlab.reversed.first,
  '${Languages.elementAt(18)}: ${objective_c.reversed.first}': objective_c.reversed.first,
  '${Languages.elementAt(19)}: ${perl.reversed.first}': perl.reversed.first,
  '${Languages.elementAt(20)}: ${php.reversed.first}': php.reversed.first,
  '${Languages.elementAt(21)}: ${powerShell.reversed.first}': powerShell.reversed.first,
  '${Languages.elementAt(22)}: ${python.reversed.first}': python.reversed.first,
  '${Languages.elementAt(23)}: ${r.reversed.first}': r.reversed.first,
  '${Languages.elementAt(24)}: ${ruby.reversed.first}': ruby.reversed.first,
  '${Languages.elementAt(25)}: ${rust.reversed.first}': rust.reversed.first,
  '${Languages.elementAt(26)}: ${scala.reversed.first}': scala.reversed.first,
  '${Languages.elementAt(27)}: ${shell.reversed.first}': shell.reversed.first,
  '${Languages.elementAt(28)}: ${sql.reversed.first}': sql.reversed.first,
  '${Languages.elementAt(29)}: ${swift.reversed.first}': swift.reversed.first,
  '${Languages.elementAt(30)}: ${tex.reversed.first}': tex.reversed.first,
  '${Languages.elementAt(31)}: ${text.reversed.first}': text.reversed.first,
  '${Languages.elementAt(32)}: ${toml.reversed.first}': toml.reversed.first,
  '${Languages.elementAt(33)}: ${typeScript.reversed.first}': typeScript.reversed.first,
  '${Languages.elementAt(34)}: ${yaml.reversed.first}': yaml.reversed.first,
  Languages.elementAt(35): empty.reversed.first,

  // Languages.elementAt(36): 1,
};

final savedDataMap = <String, double>{
  Languages.elementAt(35): 0.1,
};

double length = dataMap.values.sum;

final totals = <double>{
  dataMap.values.elementAt(0),
};
